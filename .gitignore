# Python bytecode (generated by compileall / test runs)
__pycache__/
*.py[cod]

# Note: data/*.csv is deliberately NOT ignored. Under the FSAD principle the
# operational logs are versionable artifacts; commit them or not per operator
# preference.
